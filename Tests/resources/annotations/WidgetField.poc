@WidgetField(name="state", type=Document)
widget ReactWidget;

widget Page extends ReactWidget {

	Html method render () {
		return <div stuff={state.stuff}>{state.stuff}</div>;
	}
	
}

widget Container {

	Html method render () {
		return <Page/>;
	}
}